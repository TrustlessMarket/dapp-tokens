/* eslint-disable @typescript-eslint/no-explicit-any */

import {Box} from "@chakra-ui/react";

const ProposalResult = ({title, result}: any) => {
  console.log('title, result', title, result);
  return (
    <Box>
      ProposalResult
    </Box>
  );
}

export default ProposalResult;