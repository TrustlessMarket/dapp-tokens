import { ChakraProviderProps } from "@chakra-ui/react";

const ChakaDefaultProps: ChakraProviderProps = {
  toastOptions: {
    defaultOptions: {
      position: "bottom",
    },
  },
};

export default ChakaDefaultProps;
